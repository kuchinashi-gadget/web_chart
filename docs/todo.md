# Chart Playback TODO

## 完了済み

### 優先度A

- 日足から週足・月足へ切り替えたあと、日足へ戻した時の日付ズレを修正。
- 上位足では、基準日より未来の未完成足を表示しないように調整。
- チャートメモをクリックしたローソク足の日付に紐づけるように修正。
- 文章だけのチャートメモも表示されるように修正。
- 表示外に出たチャートメモの対象日を、表示範囲の左端または右端に寄せて扱うように修正。
- 足種バッジと「チャートプレイバック（仮）」の小表示は、画面上で邪魔になったため削除。

### 優先度B-1

- 表示本数設定を追加。
  - 毎回自動
  - 前回を引き継ぐ
  - 固定本数
- 初期表示位置設定を追加。
  - 最新
  - 最新からN本前
  - 前回の続き
  - 最初の方
- 銘柄・足種ごとに表示本数と右端日付を localStorage に保存。

### 優先度B-2

- 売買ログを見やすく整理。
- 建玉ログと返済ログの対応関係を hover / click でハイライト。
- 売買ログの個別削除を追加。
- 返済ログを削除した場合、その返済で閉じた建玉を復活。
- 建玉ログを削除した場合、紐づく返済ログも一緒に削除。
- 削除対象の返済ログに別の建玉が混ざっていた場合、その別建玉は復活。
- 勝敗集計を追加。
- 引き分け基準を設定化。
  - 初期値は損益率 0.5% 未満。
- 勝敗の表示/非表示を、損益の表示/非表示と同じ見た目に統一。

### 優先度C-1: ペイント練習のテキスト入力改善

- テキストツール選択時、クリックした位置に入力欄を表示。
- 入力欄に自動でフォーカスするように修正。
- Enter で追加できるように修正。
- 入力中に他の場所をクリックしても、入力内容がすぐ消えないように改善。
- 追加後のテキスト背景を透明化。
- テキストサイズとフォントを選択可能にした。
- テキストサイズ・フォントは、テキスト入力欄の近くに表示する形に変更。
- 追加後のテキストをドラッグ移動できるようにした。
- テキストが横長に見えすぎないように描画バランスを調整。

### 優先度C-2: Ctrl+Z対応

- ペイント練習中の元に戻すを `Ctrl+Z` に対応。
- やり直しを `Ctrl+Y` / `Ctrl+Shift+Z` に対応。

### 優先度C-3: 色履歴とテキスト色保持

- 描画ツールごとに前回使った色を保持。
- テキスト色も前回使った色を保持。
- 追加したカスタム色を履歴として扱いやすくした。

### 優先度C-5: 本数計測機能

- チャート上でローソク足の本数を数える「本数計測」モードを追加。
- 本数計測ボタンを押してから、始点と終点のローソク足をクリックすると本数を表示。
- 計測範囲を黄色の帯とラベルで表示。
- 3回目以降のクリックで新しい計測を開始。
- `Esc` で計測をクリア。
- PC向けに `C` キーで本数計測モードをオン/オフできるショートカットを追加。

## 優先度C: 未実装・保留

### C-4. 音素材の差し替え

- 現在の電子音を、効果音素材へ差し替える予定。
- スクショ取得、注文、約定、エラーなどで音を出す。
- ミュート設定は維持する。
- 候補サイト: https://soundeffect-lab.info/sound/button/

### C-6. 練習結果サマリー

- 練習終了時に結果をまとめる。
- 表示候補:
  - 勝ち数
  - 負け数
  - 引き分け数
  - 総損益
  - 最大利益
  - 最大損失
  - 売買回数
  - 練習期間

## 後回し候補

- 練習モード分離。
  - 気軽に練習: 過去に戻れる、履歴削除可。
  - 真剣練習: 過去に戻れない、履歴削除不可。
- 会員登録、クラウド保存。
- App.tsx の分割。
- スマホ向けUIの本格最適化。
- 公開サイトの使い方ページ、免責事項ページの整備。

## 作業ルール

- 1回の Codex 依頼は小さめに区切る。
- `npm run build` と `npm run lint` が通ったら、こまめに git へ保存する。
- 迷ったら実装前に仕様を短く確認する。
- App.tsx が大きいため、無関係なリファクタは混ぜない。
